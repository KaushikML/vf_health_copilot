# Local development only. Leave blank until you know the correct values.
# Do not commit real secrets.

APP_ENV=local
VF_ENABLE_MLFLOW=false
VF_MLFLOW_EXPERIMENT=
VF_RAW_CSV_PATH=
VF_OUTPUT_DIR=.local_outputs

# Optional Databricks values for later integration.
DATABRICKS_HOST=
DATABRICKS_TOKEN=
DATABRICKS_USER=
DATABRICKS_CATALOG=
DATABRICKS_SCHEMA=
DATABRICKS_SQL_WAREHOUSE_ID=
DATABRICKS_SERVING_ENDPOINT=
